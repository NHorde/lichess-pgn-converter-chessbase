Install requirements
```pip install -r requirements.txt```

Place your .pgn from Lichess within the ```/games``` folder and run the following command line:

```python main.py {name_of_your_file}```

For example:
```python main.py game```

This will create a ```game_cleaned.pgn``` file within the ```games``` folder